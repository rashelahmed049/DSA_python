def selection_sort(l):
    if len(l) == 0: 
        print("No element is present!")
        return
    for i in range(len(l)):
        m = i
        for j in range(i+1,len(l)):
            if l[j] < l[m]:
                m = j
        l[i] = l[m]
    return l
